<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_create_room"
        tools:context=".src.main.game.GameFragment">

        <ImageView
            android:id="@+id/btn_ox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:layout_marginTop="130dp"
            android:layout_marginHorizontal="@dimen/activity_margin_default"
            android:src="@drawable/img_ox_quiz_banner"
            app:layout_constraintTop_toTopOf="parent"
            tools:layout_editor_absoluteX="16dp" />

        <ImageView
            android:id="@+id/btn_word"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:layout_marginTop="60dp"
            android:layout_marginHorizontal="@dimen/activity_margin_default"
            android:src="@drawable/img_word_quiz_banner"
            app:layout_constraintTop_toBottomOf="@+id/btn_ox"
            tools:layout_editor_absoluteX="16dp" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>